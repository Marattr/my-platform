server:
  port: 8888

spring:
  application:
    name: config-server
  security:
    user:
      name: ${CONFIG_SERVER_USER:ihs}
      password: ${CONFIG_SERVER_PASS:ihs123!}
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Marattr/config-repo
          default-label: main
          clone-on-start: true